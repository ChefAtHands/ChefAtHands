apiVersion: apps/v1
kind: Deployment
metadata:
  name: recipe-search-service
  namespace: chefathands
  labels:
    app: recipe-search-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recipe-search-service
  template:
    metadata:
      labels:
        app: recipe-search-service
    spec:
      containers:
      - name: recipe-search-service
        image: anejt/chefathands-recipe-search-service:latest
        ports:
        - containerPort: 8085
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "kubernetes"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: recipe-search-service
  namespace: chefathands
spec:
  selector:
    app: recipe-search-service
  ports:
  - protocol: TCP
    port: 8085
    targetPort: 8085
  type: ClusterIP