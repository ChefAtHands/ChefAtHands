apiVersion: apps/v1
kind: Deployment
metadata:
  name: logging-service
  namespace: chefathands
  labels:
    app: logging-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: logging-service
  template:
    metadata:
      labels:
        app: logging-service
    spec:
      containers:
      - name: logging-service
        image: anejt/chefathands-logging-service:latest
        ports:
        - containerPort: 8082
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "kubernetes"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: logging-service
  namespace: chefathands
spec:
  selector:
    app: logging-service
  ports:
  - protocol: TCP
    port: 8082
    targetPort: 8082
  type: ClusterIP