apiVersion: apps/v1
kind: Deployment
metadata:
  name: history-service
  namespace: chefathands
  labels:
    app: history-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: history-service
  template:
    metadata:
      labels:
        app: history-service
    spec:
      containers:
      - name: history-service
        image: anejt/chefathands-history-service:latest
        ports:
        - containerPort: 8088
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "kubernetes"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: history-service
  namespace: chefathands
spec:
  selector:
    app: history-service
  ports:
  - protocol: TCP
    port: 8088
    targetPort: 8088
  type: ClusterIP